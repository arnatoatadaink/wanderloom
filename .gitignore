node_modules/
# Also ignore WSL dependency symlinks used from the Windows working tree.
node_modules
.pnpm-store/
dist/
trash/

# Local Google OIDC configuration; never commit environment-specific values.
apps/web/.env.local
workers/api/.dev.vars
