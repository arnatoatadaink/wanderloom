{
  "name": "@wanderloom/api",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "build": "wrangler deploy --dry-run --outdir dist"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "1.1.12",
    "vitest": "4.1.11",
    "wrangler": "4.132.0"
  },
  "dependencies": {
    "@wanderloom/game-core": "workspace:*"
  }
}
