# A-001 Workspace validation — 2026-09-18

Status: A-001 / W0-001 / CP-01 complete locally.

Validated the workspace from `feat/a-001-workspace`, based on commit
`25daf1a53a9d3e9c882cb9c62d51fe562c321bd3`.

Environment: Windows, Node.js v22.20.0, pnpm 10.17.1, TypeScript 5.9.3.
The host default is pnpm 11.19.0, so the project-pinned version was invoked
through `pnpm dlx pnpm@10.17.1 <command>`.

| Acceptance command | Result |
| --- | --- |
| `pnpm install` | PASS; dependencies installed with pnpm 10.17.1 |
| `pnpm typecheck` | PASS for all three packages |
| `pnpm test` | PASS; Node test runner exits successfully, zero tests in each package |
| `pnpm build` | PASS; TypeScript output emitted for all three packages |

The initial install used the host pnpm version. The pinned-version install
used `CI=true` and `--no-frozen-lockfile` to recreate the dependency directory
non-interactively. The resulting lockfile records the resolved dependency.
The final `pnpm dlx pnpm@10.17.1 install --frozen-lockfile` also passed,
confirming installation with the committed-format lockfile unchanged.

Added `pnpm-lock.yaml` for repeatable installs and `.gitignore` to exclude
dependencies, local package-store data, and build output.

This validates workspace initialization only. There are no behavioral tests
yet. Vite, Vitest, Wrangler, Cloudflare Vitest plugin, production bundling,
and the final tooling configuration remain A-002 work.
