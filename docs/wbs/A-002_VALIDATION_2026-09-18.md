# A-002 Tooling validation — 2026-09-18

Status: A-002 complete locally.

## Pinned toolchain

| Tool | Version |
| --- | --- |
| pnpm | 10.17.1 |
| TypeScript | 7.0.2 |
| Vite | 8.2.2 |
| Vitest | 4.1.11 |
| Wrangler | 4.132.0 |
| @cloudflare/vitest-plugin | 1.1.12 |

Validated on Windows with Node.js 22.20.0. The host pnpm is 11.19.0;
commands below were run through `pnpm dlx pnpm@10.17.1` to use the
project-pinned version. Non-interactive dependency recreation used `CI=true`.

## Results

| Command | Result |
| --- | --- |
| `pnpm install --no-frozen-lockfile` | PASS; lockfile updated |
| `pnpm typecheck` | PASS; all three packages |
| `pnpm test` | PASS; three source test files, three tests |
| `pnpm build` | PASS; Vite frontend, TypeScript core, Wrangler dry run |

Vitest 5.0.1 initially failed to start the Cloudflare test worker. The installed
Cloudflare plugin declares `vitest: ^4.1.0` as its peer dependency. All three
packages now pin 4.1.11; the peer warning and runtime failure are resolved.
The API test runs through the existing Cloudflare plugin configuration.
See [Cloudflare testing documentation](https://developers.cloudflare.com/workers/testing/vitest-integration/).

The core Vitest configuration limits discovery to `src/**/*.test.ts`, avoiding
duplicate execution of emitted tests under `dist` after a build. Tests were
rerun after building and passed with exactly three tests across the workspace.

The installation reports ignored dependency build scripts for esbuild and
workerd. Their installed platform binaries worked in the verified test/build
commands; no general dependency-script approval was enabled.

Validation covers tooling and workspace smoke tests, not game behavior or D1.
The Worker build is a dry run; no deployment was performed. W0-002's separate
lint baseline remains pending.
