{
  "name": "@wanderloom/web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "build": "vite build"
  },
  "devDependencies": {
    "vite": "8.2.2",
    "vitest": "5.0.1"
  }
}
