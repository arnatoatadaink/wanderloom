# Google Identity local configuration example
#
# Copy to .env.local (do not commit .env.local) and replace with the OAuth
# Web application client ID from Google Cloud Console.
VITE_GOOGLE_CLIENT_ID=
